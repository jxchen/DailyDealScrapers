grouponScraper.py takes a snapshot of current groupon deals and stores
the data in tab-separated format
the fields scraped include:
    deal location
    deal title
    groupon price
    groupon value
    total quantity sold
    deal url

to run: python grouponScraper.py <filename>
the default filename is grouponData.tsv

There is some example data in grouponData.tsv that I collected around 1PM EDT on
6/3/11.
